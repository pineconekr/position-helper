# Database Configuration
# Get your Neon PostgreSQL connection string from https://neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Alternative: Netlify Database URL (takes precedence over DATABASE_URL if both are set)
# NETLIFY_DATABASE_URL=postgresql://user:password@host/database?sslmode=require
